# Uncomment the next line to define a global platform for your project
platform :ios, '11.0'

target 'MamaWeather' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for MamaWeather
  pod 'Alamofire', '~> 4.6'
  pod 'SwiftyJSON', '~> 4.0'
  pod 'Cache', '~> 4.1'
end
